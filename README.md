### Install Docker Desktop
### Run with "docker-compose up -d"  
Once the project is running, execute the task with "docker exec -it spark-master spark-submit /app/ecommerce_analysis.py --master spark://spark-master:7077"  
### Shutdown docker with "docker-compose down"
